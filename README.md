# Workshop Chrome Plugin

Базовый Chrome плагин на WXT с TypeScript для демонстрации архитектуры расширения.

## 📋 Описание

Это шаблонный проект Chrome расширения, созданный с использованием фреймворка WXT и TypeScript. Проект демонстрирует базовую архитектуру расширения с основными компонентами:

- **Background Service Worker** - фоновый скрипт для обработки событий
- **Content Script** - скрипт, внедряемый на веб-страницы
- **Popup** - всплывающее окно расширения
- **Options** - страница настроек

## 🚀 Установка

```bash
# Установка зависимостей
npm install
```

## 🛠️ Разработка

```bash
# Запуск в режиме разработки (Chrome)
npm run dev

# Запуск в режиме разработки (Firefox)
npm run dev:firefox
```

После запуска WXT автоматически создаст директорию `.output` с собранным расширением. Загрузите эту директорию в Chrome через `chrome://extensions/` в режиме разработчика.

## 📦 Сборка

```bash
# Сборка для Chrome
npm run build

# Сборка для Firefox
npm run build:firefox
```

## 📤 Создание архива

```bash
# Создание ZIP архива для Chrome
npm run zip

# Создание ZIP архива для Firefox
npm run zip:firefox
```

## 📁 Структура проекта

```
workshop-chrome-plugin/
├── entrypoints/          # Точки входа расширения
│   ├── background.ts      # Background Service Worker
│   ├── content.ts         # Content Script
│   ├── popup.html         # HTML для popup
│   ├── popup.tsx          # Логика popup
│   ├── options.html       # HTML для настроек
│   └── options.tsx        # Логика настроек
├── src/                   # Исходный код
│   ├── types/             # Типы TypeScript
│   │   └── index.ts
│   └── utils/             # Утилиты
│       ├── storage.ts     # Работа с хранилищем
│       ├── messaging.ts   # Работа с сообщениями
│       ├── constants.ts   # Константы
│       └── index.ts
├── .wxt/                  # Директория WXT (автоматически)
├── .output/               # Собранное расширение (автоматически)
├── wxt.config.ts          # Конфигурация WXT
├── tsconfig.json          # Конфигурация TypeScript
├── package.json           # Зависимости и скрипты
└── README.md              # Документация
```

## 🔧 Компоненты

### Background Service Worker (`entrypoints/background.ts`)

Выполняется в фоновом режиме и обрабатывает:
- Установку и обновление расширения
- Сообщения от content scripts и popup
- Изменения в хранилище

### Content Script (`entrypoints/content.ts`)

Внедряется на веб-страницы и:
- Создаёт индикатор расширения на странице
- Отображает статус расширения
- Реагирует на изменения настроек

### Popup (`entrypoints/popup.html` + `popup.tsx`)

Всплывающее окно при клике на иконку расширения:
- Отображает текущий статус
- Позволяет переключать статус
- Кнопка перехода к настройкам

### Options (`entrypoints/options.html` + `options.tsx`)

Страница настроек расширения:
- Переключатель включения/выключения
- Выбор темы оформления
- Кнопки сохранения и сброса

## 📜 Скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск в режиме разработки (Chrome) |
| `npm run dev:firefox` | Запуск в режиме разработки (Firefox) |
| `npm run build` | Сборка для продакшена (Chrome) |
| `npm run build:firefox` | Сборка для продакшена (Firefox) |
| `npm run zip` | Создание ZIP архива (Chrome) |
| `npm run zip:firefox` | Создание ZIP архива (Firefox) |
| `npm run compile` | Проверка типов TypeScript |

## 🔐 Разрешения

Расширение использует следующие разрешения:
- `storage` - для хранения настроек
- `tabs` - для работы с вкладками
- `<all_urls>` - для работы на всех веб-страницах

## 📝 Типизация

Проект использует TypeScript для строгой типизации:
- Типы настроек в `src/types/index.ts`
- Типы сообщений для коммуникации между компонентами
- Полная поддержка IntelliSense

## 🎨 Темы

Поддерживаются две темы оформления:
- **Светлая** (light) - по умолчанию
- **Тёмная** (dark)

Тема влияет на внешний вид индикатора на странице и элементов интерфейса.

## 📚 Полезные ресурсы

- [WXT Documentation](https://wxt.dev)
- [Chrome Extension Documentation](https://developer.chrome.com/docs/extensions/)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)

## 📄 Лицензия

ISC
